specVersion: "v1beta"
serviceId: "venv"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "venv-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
